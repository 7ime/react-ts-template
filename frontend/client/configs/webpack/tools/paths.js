const static = 'static';
const media = static + '/media';
const fonts = static + '/fonts';
const dist = 'dist';
const source = 'src';

module.exports = {
    static,
    media,
    fonts,
    dist,
    source
};
